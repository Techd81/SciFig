请持续执行，不要只给计划。请直接分析项目根目录下的 `templete` 目录，逐篇学习其中所有 Markdown 文章，并按照“学习 → 复刻 → 对比 → 沉淀到 SciFig”的流程执行。

任务目标不是简单总结 Markdown，而是把文章中的科研图审美、代码方法、图片设计规律，真正转化为 SciFig 的 skills、规范文档、示例代码和质量检查体系。

执行流程：

1. 扫描 `template` 下所有 `.md` / `.markdown` 文件，建立完整清单，不能跳过。
2. 逐篇阅读 Markdown，提取：

   - 文章主题
   - 图片链接
   - 代码块
   - 绘图思路
   - 配色方法
   - 布局方式
   - 字体、字号、线宽
   - legend 放置方式
   - annotation 标注方式
   - 导出方式等等
3. 对每篇 Markdown 中有代表性的图进行复刻：

   - 如果原文有代码，先基于原代码复刻。
   - 如果原文只有图片和讲解，则根据图片链接、文章上下文和描述重新写代码复刻。
   - 如果图片链接无法直接读取，要如实记录，并根据 Markdown 上下文尽量复刻其设计思路。
   - 复刻不是简单照抄，而是尽量还原其视觉冲击力、布局、配色、层次感和论文图风格。
4. 每复刻一张图，都要进行对比分析：

   - 原图强在哪里
   - 复刻图差在哪里
   - 配色是否接近
   - 布局是否规整
   - legend 是否遮挡绘图区
   - annotation 是否专业
   - 字体和字号是否统一
   - 是否达到论文级导出质量
5. 将复刻经验沉淀进 SciFig：

   - 把可复用代码封装到 examples 或工具函数中。
   - 把可复用设计规则写入 skills。
   - 把通用视觉规范写入 `docs/scifig_visual_standard.md`。
   - 把每篇文章的学习与复刻记录写入 `docs/template_learning_notes.md`。
   - 把图形质量检查规则写入或更新

核心要求：

- 不允许只总结不复刻。
- 不允许只复刻不沉淀。
- 不允许跳过 Markdown。
- 不允许忽略图片链接。
- 不允许忽略代码块。
- 不允许编造已经读取了无法访问的图片。
- 不允许把默认 matplotlib 风格当成最终效果。
- legend 绝不能遮挡绘图区。
- 多面板图必须对齐整齐。
- annotation 不能遮挡数据。

最终请给出：

1. 学习了多少篇 Markdown
2. 提取了多少个图片链接
3. 分析了多少个代码块
4. 复刻了多少张代表性图
5. 每张复刻图与原图的差距
6. 新增或修改了哪些文件
7. 哪些复刻经验已经沉淀进 SciFig 的 skills、规范、examples 和质量检查脚本

请像真的在训练和迭代一个科研绘图框架一样执行：先学模板，再复刻高质量图，再把复刻出来的经验转化为 SciFig 的稳定能力。
