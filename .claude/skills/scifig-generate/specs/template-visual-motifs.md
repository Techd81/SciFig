# Template Visual Motifs

Distilled from the `template/**/*.md` reference cases. These are visual grammar motifs, not new chart keys. Phase 2 infers them from data roles and existing chart families; Phase 3 renders them through helper overlays; Phase 4 verifies that required motif counters were actually applied.

## Core Motifs

| Motif | Data cues | Rendering pattern | QA counters |
|-------|-----------|-------------------|-------------|
| `joint_marginal_grid` | actual/predicted pairs, dense x/y diagnostics, residual diagnostics | Main scatter plus tightly attached top/right sidecar axes with Teal `#69b3a2` histograms/KDE strips outside the data marks | `marginalAxesCount`, `subAxesCount`, `templateMotifCount` |
| `train_val_marginal_ols_ci` | actual/true + predicted/fitted values with train/validation split and Statsmodels/OLS/confidence-interval cues | Case-096 2x2 marginal diagnostic: one main train/validation scatter shares split colors with top true-value and right predicted-value histograms, the top-right GridSpec cell is blank, train and validation get separate OLS fit lines plus 95% mean-confidence bands, a dotted 1:1 line anchors agreement, and one metric text box reports split-specific equations/R2/RMSE/MAE | `templateMotifCount`, `mainScatterCount`, `splitCount`, `topHistogramCount`, `rightHistogramCount`, `sharedAxisSidecarCount`, `blankCornerAxis`, `olsFitLineCount`, `olsCiBandCount`, `oneToOneLineCount`, `metricTextBoxCount` |
| `contour_marginal_validation` | hyperparameter search grid with score/error metric plus validation actual/predicted values | Case-048 validation composite: left RMSE `tricontourf` over searched hyperparameter space with an optimum marker and colorbar; right parity scatter with y=x reference, R2/RMSE metric box, and shared-axis top/right marginal histograms | `templateMotifCount`, `contourPanelCount`, `contourfCount`, `optimumMarkerCount`, `colorbarSlotCount`, `marginalAxesCount`, `referenceLineCount`, `metricBoxCount` |
| `joint_residual_marginal_performance` | actual/experimental + predicted/fitted values, residuals, train/test split, and R2/RMSE metrics | Case-051 four-axis diagnostic: central actual-vs-predicted parity scatter, top actual-value KDE, right predicted-value KDE, bottom residual-vs-actual panel sharing x with the main axes, train `#1F77B4` / test `#FF7F0E` split colors, dashed y=x and y=0 references, and an in-axes metric plaque | `templateMotifCount`, `marginalAxesCount`, `residualDiagnosticPanelCount`, `perfectFitLineCount`, `zeroResidualLineCount`, `metricBoxCount`, `sharedXAxis`, `splitHueCount` |
| `density_encoded_scatter` | point count >= 12 and numeric x/y | Points colored by local KDE or 2D-bin density, sorted ascending before draw; CEJ marginal-joint default is `GnBu_r` | `densityColorEncodingCount`, `densityHaloCount` |
| `prediction_diagnostic_matrix` | `actual` + `predicted`, optional split/model/sample columns | Perfect-fit line, R2/RMSE/MAE table, residual/error sidecar when available, sample marker shapes | `metricTableCount`, `referenceLineCount`, `sampleEncodingCount` |
| `prediction_experiment_external_stats` | sample index plus actual/experimental and predicted/fitted values, optional train/test split, and supplied R2/RMSE metrics | Case-058 Materials-style sample-index diagnostic: Actual square markers and Predicted circle markers overlay on one x-index axis, a dashed vertical divider separates Training and Testing samples, two manually constructed `Line2D` proxy legends sit at opposite upper corners, and a red `transAxes` metric panel with `clip_on=False` keeps R2/RMSE outside the dense data area | `templateMotifCount`, `actualPredictedOverlayCount`, `trainTestDividerCount`, `proxyLegendCount`, `externalStatsBox`, `metricBoxCount`, `whiteMarkerEdgeCount`, `sampleEncodingCount` |
| `target_aligned_descriptor_scatter_matrix` | one shared target/response column plus multiple numeric descriptor/feature columns with incompatible units | Case-042 2x3 descriptor matrix: every panel maps one descriptor on its native x-axis against the same target y-axis, uses `sharey=True`, semi-transparent blue points with white edges, light dashed grids behind points, despined top/right axes, and y labels only on the left column | `templateMotifCount`, `sharedYAxis`, `descriptorPanelCount`, `scatterPanelCount`, `sampleEncodingCount` |
| `multi_model_parity_grid` | actual/experimental + predicted/fitted values with a model/algorithm facet column and per-model metrics | Case-043 2x3 parity grid: one panel per model, shared x/y limits, equal aspect, solid black y=x reference in every panel, fixed red dashed +/- tolerance bands, and compact R2/RMSE metric boxes inside each panel | `templateMotifCount`, `parityPanelCount`, `perfectFitLineCount`, `toleranceBandLineCount`, `metricBoxCount`, `sharedAxisScale` |
| `parity_ci_matrix` | actual/predicted pairs + task/panel + train/test split columns | Case-083 2x2 isometric parity board with a maroon 45-degree bisect line below statistical layers, Training blue and Testing red hollow markers, per-split regression line plus 95% CI shadow band, R2/RMSE metric box, and panel letters in `transAxes` | `templateMotifCount`, `referenceLineCount`, `ciBandCount`, `metricBoxCount`, `panelLabelCount`, `equalAspectPanels`, `splitCount` |
| `nested_marginal_joint_matrix` | model/algorithm + train/test split + actual/predicted columns | Case-084 2x3 outer model matrix; each model cell nests a 2x2 `GridSpecFromSubplotSpec` system with main parity scatter, top/right marginal KDE strips, shared red/blue train-test encoding, grey identity line, and an R2/MAE/RMSE plaque | `templateMotifCount`, `modelPanelCount`, `marginalAxesCount`, `subAxesCount`, `metricBoxCount`, `referenceLineCount`, `sharedLegendCount` |
| `gam_log_residual_diagnostic` | GAM/spline/log relationship plus residual/anomaly cues, or `gam_residual_diagnostic` pattern | Nature-style 1x2 board: log-log smooth relationship with black confidence band, paired residual panel, shared gray/green/yellow anomaly classes, sparse italic R2 text | `templateMotifCount`, `confidenceBandCount`, `residualDiagnosticPanelCount`, `r2AnnotationCount`, `panelLabelCount` |
| `inset_raincloud_residual` | actual/true + predicted columns, optional system/material panel column, and residual/error + inset/raincloud cues | MGEA-style 1x2 true-vs-predicted trajectory board; orange square true line and green circle predicted line, lower-left metric text, upper-left legend, and a white residual raincloud inset in each main axes | `templateMotifCount`, `insetCount`, `insetRaincloudCount`, `metricTextCount`, `panelLabelCount` |
| `density_parity_matrix` | actual/observed/measured + predicted/fitted columns with panel/property/task groups and density/KDE/parity cues | Case-015 1x2 2D-KDE parity matrix: each property panel density-sorts points, colors by local KDE with `jet`/`viridis`, overlays a red 1:1 line, places R2/RMSE in a white transAxes box, and attaches a vertical Density colorbar per panel | `templateMotifCount`, `densityParityPanelCount`, `densityColorEncodingCount`, `referenceLineCount`, `metricBoxCount`, `colorbarSlotCount`, `sampleEncodingCount` |
| `density_linear_regression_fit` | true/predicted or observed/estimated numeric pairs with density/KDE plus linear-regression and 1:1 agreement cues | Case-092 single-panel density regression: density-sorted `gaussian_kde` scatter uses source-like `seismic_r`, thin black point edges, a neutral gray OLS fit, a blue dashed 1:1 line, an upper-left Number/equation/R2/RMSE/MAE text block, a narrow right H/M/L density colorbar, and inward major/minor ticks on all four sides | `templateMotifCount`, `densityScatterCount`, `pointCount`, `densitySortedPoints`, `fitLineCount`, `oneToOneLineCount`, `metricTextCount`, `colorbarCount`, `hmlLabelCount`, `fourSideTicks` |
| `model_region_density_parity_matrix` | actual/observed + predicted/fitted columns crossed by model rows and region/target columns with density/KDE/parity cues | Case-060 3x5 model-by-region density matrix: `GridSpec(3,6)` uses five equal data columns plus a narrow row-colorbar column; every cell density-sorts KDE-colored scatter, keeps equal aspect and shared limits, overlays red dashed 1:1 reference, blue regression fit with CI band, and bold R2/MAE/RMSE text | `templateMotifCount`, `densityParityPanelCount`, `modelRowCount`, `regionColumnCount`, `rowColorbarCount`, `densitySortedPoints`, `referenceLineCount`, `fitLineCount`, `confidenceBandCount`, `metricTextCount`, `equalAspectPanels` |
| `hydrology_density_parity_matrix` | observed/MODIS ET + simulated/model ET crossed by basin/region rows and model/mechanism columns with KDE/density/parity cues | Case-069 3x3 shared-axis hydrology matrix: every cell density-sorts `RdBu_r` KDE scatter, overlays a red dashed 1:1 line and black fitted regression line, places bottom-right NSE/r text, attaches a local right colorbar, and keeps common observed/simulated axis limits such as `[0, 45]` | `templateMotifCount`, `densityParityPanelCount`, `basinCount`, `modelCount`, `densitySortedPoints`, `referenceLineCount`, `fitLineCount`, `metricTextCount`, `localColorbarCount`, `sharedAxisLimits` |
| `time_series_prediction_interval` | time/sample index + actual/observed/true + predicted/fitted values plus PI lower/upper and optional train/test split | Case-016 train/test diagnostic: sky-blue 90% prediction-interval band, black observed dots, red prediction line, gray dashed split divider, region labels, and top horizontal legend | `templateMotifCount`, `intervalBandCount`, `observedScatterCount`, `predictedLineCount`, `trainTestDividerCount`, `referenceLineCount`, `sampleEncodingCount` |
| `trend_distribution_hybrid` | time/year x-axis plus scenario/group trajectories with lower/upper uncertainty and terminal replicate draws/distribution values | Case-056 7-panel pollutant board: `GridSpec(2,4)` activates four panels on the top row and three on the bottom row; each panel extends the time axis beyond the forecast horizon, draws translucent scenario `fill_between` bands under line+marker means, then places scenario-colored terminal boxplots at artificial x positions such as 2064/2066/2068 with one shared legend | `templateMotifCount`, `activePanelCount`, `intervalBandCount`, `trendLineCount`, `terminalBoxplotCount`, `unusedGridCellCount`, `sharedLegendCount`, `xAxisExtension` |
| `prediction_residual_stack` | sample/time index + observed/experimental values + predicted/fitted values + residuals, with optional train/test split | Case-044 vertical stack: top panel overlays experimental and predicted line-marker series, bottom panel shows residual scatter on the same x-axis, a zero-residual reference line anchors the lower panel, and one train/test split line is repeated through both panels | `templateMotifCount`, `predictedLineCount`, `observedLineCount`, `residualPointCount`, `zeroResidualLineCount`, `trainTestDividerCount`, `sharedXAxis` |
| `biodegradation_kinetics_validation_board` | long-term operation days plus influent concentration/removal efficiency, short-term kinetic time series with errors, inhibitor/control groups, and zone/stage distribution values | Case-079 2x2 microcosm validation board: panel A uses twinx scatter plus removal lines for long-term operation; panels B/C use errorbar lines for concentration decay and removal kinetics; panel D uses boxplots with raw replicate overlays for stage summaries | `templateMotifCount`, `panelCount`, `dualAxisPanelCount`, `kineticErrorbarPanelCount`, `boxplotPanelCount`, `twinAxisCount`, `errorbarSeriesCount`, `rawPointOverlay` |
| `multimetric_model_boxplot` | model/algorithm + repeated fold/resampling metric values + train/test split + two or more metrics such as MAE/RMSE/R2 | Case-045 1x3 metric board: one grouped boxplot panel per metric, independent y-axis scales, stable Training `#4A90E2` and Testing `#F5A623` hue mapping, and one top-center figure legend instead of repeated per-panel legends | `templateMotifCount`, `boxplotPanelCount`, `metricPanelCount`, `groupedBoxCount`, `splitHueCount`, `sharedLegendCount`, `independentYAxis` |
| `grouped_box_median_regression` | ordinal interval/category column plus repeated numeric values for two or more series, with trend/regression/K/R cues | Case-093 single-axis grouped box trend: each interval contains left/right offset boxplots, alternating vertical background bands replace the grid, medians are extracted per interval and fitted with series-colored dashed regression lines, a combined box-plus-line legend sits lower-left, and matching `k` / `R` annotations sit upper-right | `templateMotifCount`, `intervalCount`, `boxSeriesCount`, `boxCount`, `medianRegressionLineCount`, `regressionBasis`, `combinedLegendCount`, `legendItemCount`, `krAnnotationCount`, `alternatingBandCount` |
| `gradient_box_metric_dashboard` | model/algorithm + repeated metric values for two or more metrics, especially R2/MAE/RMSE, plus gradient-box wording | Case-076 2x2 JBE metric dashboard: three active metric panels use independent y-scales and stable algorithm colors; every group manually layers hollow jitter points, vertical `imshow` gradient boxes, whiskers, median lines, and pink square mean markers; the lower-right cell is a custom legend panel | `templateMotifCount`, `gradientBoxImshow`, `boxplotPanelCount`, `metricPanelCount`, `legendPanelCount`, `jitterPointCount`, `meanMarkerCount`, `medianLineCount`, `independentYAxis` |
| `stacked_violin_metric_board` | model/algorithm + repeated metric values for several incompatible metrics such as R2, MAE, and RMSE | Case-074 3x1 stacked violin board: shared model x-axis, independent metric y-scales, zero hspace, full violin distributions, narrow white boxplots, red median points, color-matched median labels, and dashed separator spines | `templateMotifCount`, `panelCount`, `metricPanelCount`, `modelCount`, `violinLayerCount`, `boxplotLayerCount`, `medianPointCount`, `medianLabelCount`, `sharedXAxis`, `independentYAxis` |
| `feature_importance_bar_board` | ranked feature-importance values plus condition-wise feature-group percentage contributions, especially MDA / importance / pressure / temperature tables | Case-068 2x2 materials-informatics board: left-column horizontal MDA/importance rankings pair with right-column stacked percentage bars, x positions such as `[0,1,2,4,5,6]` create a physical-condition gap, bottom brackets label `0 deg C` and `25 deg C`, and P/PT/CC stacked colors stay stable across both percentage panels | `templateMotifCount`, `panelCount`, `horizontalBarCount`, `stackedPanelCount`, `stackedBarSegmentCount`, `bottomBracketCount`, `xGapPositions`, `legendCount` |
| `polar_waffle_feature_importance` | feature-level importance/SHAP values plus a feature-group/category mapping and grouped contribution percentages | Case-086 asymmetric explainability board: left polar axis spans both rows and renders mean `|SHAP|` as radial bars with hand-drawn concentric/radial grid; right panel renders a 10x10 waffle grid where every square is one percent of group contribution, with the group legend reserved in the lower-right row | `templateMotifCount`, `polarBarCount`, `waffleSquareCount`, `groupCount`, `waffleRoundingSum`, `polygonPolarGrid`, `panelLabelCount` |
| `urban_gradient_flux_boxplots` | gas/panel column such as CH4/CO2, urbanization-gradient categories, season/split hue, and positive flux values that span orders of magnitude | Case-049 1x2 environmental flux board: one panel per gas, Low/Medium/High urbanization on x, Cool-dry `#4C72B0` and Warm-wet `#DD8452` grouped boxes inside each category, log y-axis on both panels, independent gas-specific y scales, and one shared season legend | `templateMotifCount`, `boxplotPanelCount`, `groupedBoxCount`, `logScaleYAxisCount`, `splitHueCount`, `sharedLegendCount`, `independentYAxis` |
| `greenhouse_flux_raincloud` | grouped environmental flux values with gas/metric panel column and urbanization/category groups | Case-061 Water Research-style two-panel raincloud board: CH4 and CO2 flux panels use independent y scales over the same urbanization x groups, each group layers a half-violin KDE cloud, white-edged jittered raw observations, and a narrow transparent boxplot summary; legends are removed and category label spacing is protected with `subplots_adjust` | `templateMotifCount`, `raincloudPanelCount`, `halfViolinCount`, `jitterPointCount`, `boxplotCount`, `raincloudLayerOrder`, `independentYAxis`, `legendRemoved`, `whiteMarkerEdgeCount` |
| `cell_marker_bar_scatter` | marker/protein/category percentages with raw sample values, mean, error/SD/SEM, and dorsal/ventral or other block labels | Case-077 Cell-style single panel: non-uniform x positions create a semantic gap between marker blocks; mean bars sit under black error bars and jittered raw sample points; top horizontal rules and bold labels identify dorsal/ventral groups without acting as significance brackets | `templateMotifCount`, `barCount`, `errorbarCount`, `jitterScatterLayerCount`, `nonUniformXGap`, `topGroupRuleCount`, `groupLabelCount`, `sampleEncodingCount` |
| `shap_mean_importance_triptych` | target/task/panel column + feature identifiers + precomputed mean absolute SHAP values; optional target names such as SSA/Vt/NC | Case-046 1x3 global-importance triptych: one horizontal mean `|SHAP|` bar panel per target, independent x-axis scales, local `RdYlBu_r` normalization per panel, despined axes, x-grid behind bars, and numeric SHAP value labels on each bar | `templateMotifCount`, `shapBarCount`, `targetPanelCount`, `numericLabelCount`, `independentXAxis`, `localColorNormalization`, `panelLabelCount` |
| `shap_dependence_background_grid` | feature identifier + feature value + SHAP value in long form, with SHAP dependence / signed effect cues | Case-017 2x3 SHAP dependence grid: each feature panel has pale red positive and pale blue negative SHAP background zones, a dashed zero line, black alpha scatter, uniform y-limits, and bold feature x-labels | `templateMotifCount`, `shapDependencePanelCount`, `backgroundZoneCount`, `zeroReferenceLineCount`, `referenceLineCount`, `sampleEncodingCount` |
| `shap_interaction_dependence_grid` | feature identifier + feature value + SHAP value + interaction/secondary feature value in long form | Case-018/039 SHAP dependence grid: each feature panel color-maps scatter points by a secondary interaction feature, keeps a zero SHAP baseline, and uses either slim per-panel colorbars or one global interaction-value colorbar; Case-039 adds quadratic trend lines and light CI bands in a 3x2 layout | `templateMotifCount`, `shapInteractionPanelCount`, `shapInteractionScatterCount`, `colorbarSlotCount`, `zeroReferenceLineCount`, `panelLabelCount`, `confidenceBandCount`, `trendLineCount` |
| `shap_dependence_conversion_matrix` | target/task rows such as CH4/CO2 conversion + main feature columns + SHAP contribution + panel-local secondary interaction feature | Case-050 2x2 target-by-feature SHAP dependence matrix: rows are prediction targets, columns are main features, each panel has interaction-colored `RdYlBu_r` scatter, black nonlinear trend line, gray dashed y=0 SHAP baseline, and an independent Secondary Feature colorbar | `templateMotifCount`, `shapDependencePanelCount`, `shapInteractionScatterCount`, `targetPanelCount`, `featurePanelCount`, `trendLineCount`, `zeroReferenceLineCount`, `colorbarSlotCount`, `localColorNormalization` |
| `shap_pdp_threshold_panel_array` | long SHAP/PDP table with feature id, feature value, SHAP contribution, optional interaction value, and threshold/cutoff annotations | Case-036 RF-XGB SHAP PDP board: 1xN feature panels with y=0 SHAP baseline, nonlinear smooth dependence trend, interaction-value colorbars, and threshold callouts attached to supported data regions | `templateMotifCount`, `shapDependencePanelCount`, `zeroReferenceLineCount`, `pdpTrendLineCount`, `thresholdAnnotationCount`, `colorbarSlotCount` |
| `pdp_ice_threshold_grid` | Random Forest/RFR PDP and ICE outputs by feature, with optional threshold values, zoom-in panels, and source-data rug distributions | Case-089 2x3 mechanism grid: each panel draws many gray ICE curves under one thick blue dashed PDP line, marks a mechanism threshold with a red dotted segment and bold label, adds bottom rug ticks via x-axis transform, and reserves lower-row panels for local zoom-ins | `templateMotifCount`, `panelCount`, `iceLineCount`, `pdpLineCount`, `thresholdLineCount`, `thresholdLabelCount`, `rugTickCount`, `zoomPanelCount` |
| `pdp_interaction_contour_matrix` | two-feature PDP/partial-dependence response surfaces, interaction grids, or control-boundary wording | Case-062 2x2 2D-PDP interaction matrix: every panel uses `contourf` with shared `viridis` levels for the predicted response surface, overlays Q1 blue dash-dot, median green solid, and Q3 red dashed contour lines, and uses one outside `Predicted Target` colorbar for cross-panel comparison; Case-078 is the XGBoost/`partial_dependence` 1x3 variant when exactly three feature pairs and exportable PDP grids are supplied | `templateMotifCount`, `pdpContourPanelCount`, `contourfCount`, `quartileContourLineCount`, `sharedColorbarCount`, `sharedContourLevels`, `controlBoundaryLineCount`, `pdpGridExported` |
| `pdp_3d_surface_panel` | two-feature PDP/partial-dependence grid with x-grid, y-grid, and predicted response matrix, especially when 3D surface, bottom contour projection, or `plot_surface` is requested | Case-087 single 3D PDP panel: one `projection="3d"` axes renders a `viridis` `plot_surface` response with `alpha=0.85`, a bottom `contourf` projection at `z_min`, transparent panes, dashed grid, `view_init(elev=30, azim=-60)`, `set_box_aspect((1.05,1.0,0.78))`, and one right colorbar labeled `Prediction Response` | `templateMotifCount`, `surfaceCount`, `contourProjectionCount`, `colorbarCount`, `transparentPanes`, `viewElev`, `viewAzim`, `pdpGridShape` |
| `ml_model_performance_triptych` | `model` / `algorithm` plus train/test, validation, R2/RMSE/MAE/AUC/F1, actual/predicted, residual, or relative-error fields | RF/GBDT diagnostic triptych: either top-row model benchmark with parity/residual support, or Case-040 horizontal evidence chain with material grouped bars, observed-vs-predicted grouped bars plus relative-error twin-axis line, and error boxplots overlaid with raw hollow-square points | `templateMotifCount`, `metricTableCount`, `referenceLineCount`, `dualAxisEncodingCount`, `boxplotPanelCount`, `sampleEncodingCount` |
| `model_accuracy_stability_board` | model/algorithm columns plus true/predicted train-test values and repeated-run stability metrics such as `R2`, `RMSE`, `STD`, Monte Carlo, or random-test repeats | Case-081 2x3 board: each model column pairs a top nested marginal-joint parity panel with a bottom `R2`-`RMSE` Monte Carlo stability scatter; top cells use train/test points, one-to-one line, and top/right KDE sidecars; bottom cells color stability points by spread and add computed mean crosshairs plus stat boxes | `templateMotifCount`, `modelColumnCount`, `nestedMarginalPanelCount`, `stabilityScatterPanelCount`, `monteCarloPointCount`, `meanCrosshairCount`, `metricBoxCount`, `marginalAxesCount` |
| `spearman_ml_evaluation_board` | Spearman/correlation matrix plus ML explanation, actual/predicted fit rows, and external validation or error-summary fields | Case-065 2x2 manuscript evidence board: Spearman heatmap with numeric `r` labels and supplied significance stars, SHAP importance bars with cluster brackets, train/test parity scatter with hollow markers and an inset RMSE/MAE/time table, and an external validation dual-axis panel combining experimental/predicted points with RMSE or percent-error bars | `templateMotifCount`, `panelCount`, `spearmanHeatmapCells`, `significanceStarCount`, `shapBarCount`, `clusterBracketCount`, `parityPointCount`, `insetTableCount`, `externalTwinAxis`, `externalBarCount` |
| `prediction_shap_pdp_board` | actual/experimental + predicted/fitted values, residuals/errors, SHAP importance values, and one feature-level PDP/local-dependence table | Case-070 2x2 performance-to-explanation board: asymmetric `GridSpec(2,2)` with parity scatter and metric box, residual histogram, mean `|SHAP|` bars, and a PDP/dependence scatter with a smooth trend for the top mechanism feature | `templateMotifCount`, `panelCount`, `perfectFitLineCount`, `metricBoxCount`, `residualHistogramCount`, `shapBarCount`, `pdpScatterCount`, `pdpTrendLineCount`, `exportDpi` |
| `adsorption_isotherm_multipanel` | pressure/dose x-axis + uptake/capacity y-axis + condition/panel and method/model/source columns; adsorption/isotherm cues | Case-082 CEJ-style 2x3 GridSpec storyboard: OR/TR/IL model curves form the middle layer, Experiment and GCMC hollow markers sit on the top layer, dashed grids stay behind data, panel labels use `transAxes`, and per-panel legends may be consolidated into one bottom-center method legend | `templateMotifCount`, `panelCount`, `modelCurveCount`, `hollowMarkerSeriesCount`, `panelLabelCount`, `sharedLegendCount`, `sandwichLayerOrder` |
| `ttop_permafrost_raster_map` | aligned raster arrays or GeoTIFFs for land type, DDF, DDT, and a TTOP/permafrost threshold workflow | Case-090 two-panel raster map: left panel renders continuous TTOP with a diverging scale centered on zero and a dashed zero contour; right panel renders binary permafrost extent with separate classes for non-permafrost, permafrost, and NoData/unmapped pixels, plus north-arrow and scale-bar map cues | `templateMotifCount`, `continuousRasterPanelCount`, `binaryRasterPanelCount`, `zeroContourCount`, `discreteLegendCategoryCount`, `nodataPreserved`, `colorbarCount`, `northArrowCount`, `scaleBarCount` |
| `freeze_thaw_raster_parameter_atlas` | ordered daily or regular-interval ground-temperature raster stack with dates, below-zero threshold logic, cycle year, and derived freeze-thaw parameter rasters | Case-091 2x2 raster atlas: panels show freeze start day, last frozen/melt day, freeze duration, and actual frozen days; each panel keeps a local colorbar because DOY and duration units differ, NoData masks are preserved, and the cycle definition plus `TIME_STEP_DAYS` assumption are recorded | `templateMotifCount`, `parameterPanelCount`, `freezeTimePanel`, `meltTimePanel`, `freezeDurationPanel`, `actualFreezeDaysPanel`, `colorbarCount`, `nodataMaskPanelCount`, `timeStepDays` |
| `faceted_hr_forest` | outcome/disease/facet + model/adjustment tier + HR/ratio estimate with lower/upper CI columns | Nature Communications-style 1x4 shared-y forest board: aligned Model 1-3 rows, HR=1 dashed spine in each panel, asymmetric xerr whiskers, shared top-center model legend | `templateMotifCount`, `referenceLineCount` |
| `rf_classifier_report_board` | RF/Random Forest model plus score/probability, binary label, and feature importance/SHAP/gain/permutation fields | Classifier validation board plus a ranked RF feature-importance lane, compact model/threshold summary, and selected-model competition strip when multiple classifiers appear | `templateMotifCount`, `metricTableCount`, `panelLabelCount` |
| `rf_efi_shap_donut` | RF/RFR feature importance table with EFI / `feature_importances_` plus mean absolute SHAP values for the same features | Case-052 1x2 explanation board: left RF EFI horizontal bars sorted ascending with light-to-dark blue gradient and red terminal value labels; right hollow SHAP donut sorted by mean `|SHAP|`, external labels, grey collision-aware leader lines, and an explicit note that EFI and SHAP are different units | `templateMotifCount`, `barCount`, `gradientBarCount`, `numericLabelCount`, `donutWedgeCount`, `leaderLineCount`, `hollowDonut`, `crossPanelUnitWarning` |
| `classifier_validation_board` | score/probability + binary label, optional model/algorithm and selected/focus fields, AUC/F1/precision/recall, threshold tuning, or true/predicted classes | Four-panel classifier board: ROC discrimination, PR curve, calibration bins, threshold sweep with compact confusion sidecar, and selected-model legend semantics for multi-model probability tables | `templateMotifCount`, `metricTableCount`, `referenceLineCount` |
| `hyperparameter_stability_bubble_heatmap` | hyperparameter/tuning fields with repeated scores, model metric columns, and optional cost/stability metric | Case-031 2x2 tuning board: panels A-C show single-parameter replicate boxplots plus mean trend lines; panel D shows a discrete joint-search bubble heatmap with color mapped to score and size mapped to cost/stability | `templateMotifCount`, `panelLabelCount`, `boxplotPanelCount`, `meanTrendLineCount`, `bubblePanelCount`, `colorbarSlotCount`, `sampleEncodingCount` |
| `neural_training_dynamics` | epoch/step histories with training loss, validation loss, accuracy, AUC, F1, learning rate, or early-stopping fields | Multi-metric convergence curves, validation-gap shading, best-epoch vertical marker, compact final/best metric callout | `templateMotifCount`, `metricTableCount`, `referenceLineCount` |
| `neural_architecture_topology` | layer/module/component tables, source-target architecture edges, parameters, units, channels, heads, transformer/attention stages | Rounded module blocks, directed arrows, stage bands, and compact parameter/unit tags with a module/edge summary | `templateMotifCount`, `metricTableCount`, `inPlotExplanatoryLabelCount` |
| `architecture_metric_dashboard` | architecture rows or source-target edges with latency, FLOPs, memory, throughput, cost, parameter, or edge-weight columns | In-panel metric dashboard with compact bars and numeric summaries, paired with edge-weight line-width encoding when available | `templateMotifCount`, `metricTableCount` |
| `architecture_metric_storyboard` | architecture topology plus latency, FLOPs, memory, throughput, cost, parameter, or edge-weight columns | Topology hero plus separate metric-profile and edge-signal support axes; use `model_architecture_board` when available | `templateMotifCount`, `metricTableCount`, `panelLabelCount` |
| `classification_error_matrix` | `true_label` + `predicted_label`, `label` + score, classifier validation outputs | Row-normalized confusion matrix with count + row-percent labels, diagonal outline, compact accuracy / balanced-accuracy box, and largest off-diagonal callout | `templateMotifCount`, `metricTableCount`, `inPlotExplanatoryLabelCount` |
| `correlation_evidence_matrix` | correlation/covariance matrix or p-value columns | Diverging matrix, cell labels when small, p-value stars only from supplied p-values, shared colorbar outside axes | `significanceStarLayerCount`, `colorbarSlotCount` |
| `layered_heatmap_matrix` | row groups such as stations/cohorts plus two baselines per group, several model rows, several metric columns, and signed improvement/delta values | Case-055 Water Resources Management-style model-increment board: `GridSpec(4,3)` repeats two equal-width heatmaps and one 0.05-width row colorbar per site; all cells share a board-level symmetric `RdBu_r` / `TwoSlopeNorm(vcenter=0)` scale, white 1.5 pt cell separators, bold numeric labels, and mathtext alpha/beta tick labels | `templateMotifCount`, `layeredHeatmapMatrix`, `heatmapPanelCount`, `rowColorbarCount`, `divergingNormCentered`, `gridWidthRatios`, `cellAnnotationCount` |
| `upper_triangle_pie_correlation_matrix` | square correlation matrix or wide numeric table with upper-triangle, pie-glyph, local-fill, Pearson/Spearman correlation cues | Case-038 sparse upper-triangle correlation matrix: hide the lower triangle and diagonal, draw a constant-radius background circle per unique pair, fill a `Wedge` by `abs(r) * 360`, color the wedge by signed `r` on one shared [-1, 1] diverging color scale, and keep numeric labels centered | `templateMotifCount`, `upperTriangleCellCount`, `pieGlyphCellCount`, `wedgeCount`, `colorbarSlotCount` |
| `triangular_correlation_heatmap` | square correlation or raw numeric feature table with Pearson/Spearman cues, p-values/significance stars, feature groups, or triangular mask/colorbar wording | Case-073 lower-triangle correlation heatmap: one square axes with upper triangle masked, red-yellow-blue diverging cells, contrast-aware r/star annotations, external group brackets, and an oblique colorbar parallel to the matrix diagonal | `templateMotifCount`, `triangularMask`, `heatmapPanelCount`, `cellAnnotationCount`, `significanceStarCount`, `groupBracketCount`, `diagonalColorbar`, `colorbarSlotCount` |
| `bubble_correlation_matrix` | square correlation matrix, long row/column/r table, or numeric wide table plus correlation/corr/Pearson/Spearman cues | Case-011 red-blue bubble matrix: signed r encoded by teal-white-salmon `TwoSlopeNorm`, bubble area encodes `abs(r)`, every finite cell gets a centered numeric label, pale minor grid frames the symmetric matrix | `templateMotifCount`, `correlationBubbleCount`, `colorbarSlotCount`, `metricTextCount`, `sampleEncodingCount` |
| `upper_sig_lower_bubble_correlation` | square numeric feature table with Spearman/correlation plus p-value/significance/star and bubble-size legend cues | Case-097 triangular hybrid: the diagonal is blank, lower triangle draws bubbles sized by `abs(r)` and colored by signed `r`, upper triangle prints significance marker plus coefficient, one signed colorbar explains the diverging palette, and separate side legends explain `|r|` bubble size and p-value thresholds | `templateMotifCount`, `correlationMethod`, `lowerTriangleBubbleCount`, `upperTriangleTextCount`, `significanceTextCount`, `diagonalBlank`, `colorbarCount`, `bubbleSizeLegendCount`, `significanceLegendCount` |
| `spearman_kde_pairplot_matrix` | wide numeric feature table with Spearman/correlation cues plus 2D KDE, joint distribution, pairplot, or lower-triangle density-contour wording | Case-080 n x n matrix: diagonal cells are centered variable-name labels, upper triangle uses one signed Spearman color scale with numeric `r` labels, and lower triangle uses filled 2D KDE contours for pairwise distribution shape; do not route to the Nature Pearson scatter+fit matrix | `templateMotifCount`, `pairplotMatrixCellCount`, `spearmanTileCount`, `kdeContourCellCount`, `diagonalLabelCount`, `colorbarSlotCount` |
| `h_statistic_contour_dependence_board` | feature-pair interaction/H-statistic ranking plus 2D PDP or response-surface tables with sample coordinates | Case-035 asymmetric interaction board: left H-statistic horizontal ranking spans rows; right column contains top-pair `contourf` dependence panels with white black-edged sample points and independent local colorbars | `templateMotifCount`, `interactionPairCount`, `contourPanelCount`, `contourfCount`, `sampleOverlayCount`, `colorbarSlotCount` |
| `incremental_feature_selection_curve` | `model`/`algorithm` plus `n_features`, `top_k`, `feature_count`, or `ablation` with AUC/accuracy/F1/score metrics | Case-013 ML feature-selection curve: final-score-sorted multi-model lines, RF highlighted, zig-zag preserved, n=6 decision guide, y-grid only, and external right-side legend | `templateMotifCount`, `referenceLineCount`, `sampleEncodingCount`, `inPlotExplanatoryLabelCount` |
| `feature_selection_weight_performance_board` | ranked features or RFE steps with selected/removed status, feature weights, and validation score trajectory | Case-034 vertical 2x1 feature-selection board: top selected/removed red-blue importance bars, bottom R2/performance evolution line, red-star optimum marker, and arrow annotation for the chosen feature subset | `templateMotifCount`, `featureSelectionStepCount`, `rankBlockCount`, `performanceCurveCount`, `optimumMarkerCount`, `annotationCount` |
| `pca_score_dual_panel` | PCA score table with signed PC1/PC2/PC3 component contributions plus a weighted total score or ranked formulation score | Case-053 Materials Today-style 1x2 PCA score board: left positive/negative stacked PC component bars using separate `bottom_pos` and `bottom_neg` accumulators, right total-score ranking bars, shared zero baseline in both panels, polarity-aware rotated labels, and zorder sandwich grid=0 < bars=3 < zero line=4 < text=5 | `templateMotifCount`, `stackedBarSegmentCount`, `totalBarCount`, `zeroReferenceLineCount`, `polarityAwareLabelCount`, `sharedZeroBaseline`, `zorderRecipeApplied` |
| `nmds_dual_panel_grouped_scatter` | precomputed NMDS/ordination coordinates, period/panel column such as WDP/NWDP, group/color column, and optional Stress labels | Case-033 ecology ordination: 1x2 shared-y NMDS panels with identical group colors, black-edged scatter points, panel-local Stress text, light dashed grids, and one bottom-centered figure legend | `templateMotifCount`, `ordinationPanelCount`, `sharedLegendCount`, `stressAnnotationCount`, `sampleEncodingCount` |
| `pls_pm_path_model` | PLS-PM/SEM/path-model cues, or edge table roles `source` + `target` + `coefficient`/`path_coef` with optional significance and total effects | Case-014 directed topology: rounded latent-variable nodes, curved `FancyArrowPatch` paths, red/blue signed coefficients, linewidth mapped to `abs(coef)`, coefficient+star labels, GoF plaque, and upper-right horizontal total-effect inset | `templateMotifCount`, `pathEdgeCount`, `significanceLabelCount`, `insetCount`, `totalEffectBarCount`, `referenceLineCount`, `sampleEncodingCount` |
| `explainability_importance_stack` | feature + importance, SHAP, ALE/PDP, H-statistic, model explanation columns | Ranked bars/lollipops, signed zero line, low-to-high feature-value color semantics, optional dependence support panel | `referenceLineCount`, `inPlotExplanatoryLabelCount` |
| `lollipop_shap_beeswarm_board` | feature identifier + importance/gain score + SHAP value and optional feature-value column | Case-021 XGBoost explanation board: 1x2 layout with shared feature y-axis, left horizontal lollipop importance stems/points, right SHAP beeswarm, and gray dashed zero SHAP reference line | `templateMotifCount`, `lollipopLayerCount`, `shapBeeswarmCount`, `referenceLineCount`, `zeroReferenceLineCount`, `sampleEncodingCount` |
| `bipolar_lollipop_ale_board` | feature identifier + PFI/importance score + signed ALE/main-effect value | Case-022 paired lollipop board: 1x2 shared-y layout, left single-color PFI lollipop, right red/blue signed ALE lollipop, zero reference line, and hidden right-panel y labels | `templateMotifCount`, `lollipopLayerCount`, `referenceLineCount`, `zeroReferenceLineCount`, `sampleEncodingCount` |
| `mirror_radial_bar_board` | model/algorithm identifier + condition/pressure group + original-feature metric + simplified-feature metric | Case-023 mirror radial rose: single polar panel with upper/lower hemisphere condition split, cyan wide original-feature bars, yellow narrower simplified-feature overlays, rotated model labels, and an external L-shape scale axis | `templateMotifCount`, `radialBarLayerCount`, `externalScaleBarCount`, `sampleEncodingCount` |
| `hump_threshold_regression` | numeric x/y table plus threshold/breakpoint cue, or hump/threshold relationship wording | Case-024 Advanced Science threshold regression: gray bootstrap CI band, black cubic global hump fit, coral scatter, red vertical threshold anchor, cyan/green dashed segmented fits, R2 text, and outside legend | `templateMotifCount`, `confidenceBandCount`, `regressionLineCount`, `segmentedRegressionLineCount`, `thresholdLineCount`, `externalLegendCount`, `sampleEncodingCount` |
| `distance_decay_grouped_regression` | geographic distance x community similarity or beta-diversity y, with period/treatment groups such as WDP/NWDP | Case-067 ecological distance-decay panel: dense low-alpha pairwise-distance scatter, one linear fit per period, group-matched translucent `fill_between` error bands, WDP red `#D62728` and NWDP blue `#1F77B4`, colored R2/p formula text in axes coordinates, and a fit-only legend | `templateMotifCount`, `groupCount`, `scatterPointCount`, `regressionLineCount`, `errorBandCount`, `annotationTextCount`, `legendFitOnly` |
| `bayesian_ridge_heatmap_board` | condition/panel column + factor/feature column + posterior draw/effect values + correlation values | Case-025 Advanced Science composite: `GridSpec(1,5)` with width ratios `[4.2,0.35,0.6,4.2,0.35]`, paired Low/High SOC ridge panels, narrow heat strips with significance stars, and embedded horizontal heatmap colorbars | `templateMotifCount`, `ridgePanelCount`, `heatmapStripCount`, `ridgeFillCount`, `colorbarSlotCount`, `sampleEncodingCount` |
| `shap_global_local_bar_beeswarm` | long SHAP table with feature id, signed SHAP value, raw/normalized feature value, and mean absolute SHAP importance | Case-059 Journal of Environmental Chemical Engineering-style SHAP global/local board: `GridSpec(1,3)` with left mean `|SHAP|` horizontal bars, right same-order beeswarm scatter, and a narrow dedicated Low/High feature-value colorbar slot; right y labels are hidden because the left bar axis owns the shared feature order | `templateMotifCount`, `shapBarCount`, `shapBeeswarmCount`, `zeroReferenceLineCount`, `rowSeparatorCount`, `colorbarSlotCount`, `featureValueColorEncoded`, `sharedFeatureOrdering` |
| `shap_bar_beeswarm_inset_pie` | long SHAP table with feature id, SHAP value, feature value, and optional feature category/group | Case-010 SHAP composite: left mean-absolute SHAP bar panel, right density-jitter beeswarm, dedicated Low/High feature-value colorbar, and an inset contribution pie inside the bar panel | `templateMotifCount`, `referenceLineCount`, `colorbarSlotCount`, `insetPieCount`, `sampleEncodingCount` |
| `multipanel_shap_beeswarm_matrix` | SHAP arrays or long SHAP table with region/panel groups, feature ids, signed SHAP values, feature values, and mean absolute SHAP importance | Case-057 five-region SHAP matrix: `GridSpec(2,6)` spans top panels `[0:2]`, `[2:4]`, `[4:6]` and centered bottom panels `[1:3]`, `[3:5]`; each panel overlays a `twiny` purple mean `|SHAP|` bar layer behind density-jittered viridis SHAP beeswarm points, keeps a black x=0 reference, bold feature labels, and one manual board-level Feature value colorbar | `templateMotifCount`, `activePanelCount`, `twinyAxisCount`, `shapBarCount`, `shapBeeswarmCount`, `zeroReferenceLineCount`, `colorbarSlotCount`, `featureValueColorEncoded`, `sharedFeatureOrdering` |
| `shap_bar_pie_summary_board` | long SHAP table with feature id, SHAP value, feature value, and descriptor category/group | Case-019 SHAP composite: `GridSpec(2,3)` with left mean-absolute SHAP bars spanning rows, a standalone descriptor-category pie in the upper middle cell, and right SHAP summary beeswarm with a Low/High feature-value colorbar | `templateMotifCount`, `referenceLineCount`, `colorbarSlotCount`, `standalonePieCount`, `piePanelCount`, `panelLabelCount`, `sampleEncodingCount` |
| `gs_xgboost_shap_grouped_bar_beeswarm` | GS-XGBoost / XGBoost SHAP tables with many binary structure descriptors, feature groups, signed SHAP values, feature values, and mean absolute SHAP importance | Case-063 asymmetric explanation board: `GridSpec(1,2, width_ratios=[1,1.3])` places grouped mean `|SHAP|` bars on the left and same-order SHAP beeswarm points on the right; many structure descriptors may be merged into one `structures_total` row, left-lane dashed brackets label physical feature groups, the right lane uses blue-low to red-high feature-value colors, and an inset Low/High colorbar sits outside the beeswarm axis | `templateMotifCount`, `shapBarCount`, `shapBeeswarmCount`, `featureGroupBracketCount`, `mergedFeatureGroupCount`, `insetColorbarCount`, `zeroReferenceLineCount`, `featureValueColorEncoded`, `sharedFeatureOrdering` |
| `shap_molecule_global_local_parity_board` | molecular descriptor SHAP table plus internal/external actual-predicted evidence and optional split labels | Case-037 2x2 evidence board: SHAP beeswarm, global mean-absolute SHAP bars, train/test parity with metric inset, and external validation parity; SHAP panels share feature order and parity panels use perfect-fit diagonals | `templateMotifCount`, `shapBeeswarmCount`, `shapBarCount`, `perfectFitLineCount`, `metricTableCount`, `externalValidationPanelCount` |
| `environment_shap_summary_beeswarm` | long SHAP table with feature id, SHAP value, and raw feature value, especially environmental/geoscience driver wording | Case-030 single-panel SHAP summary: y lanes sorted by mean `|SHAP|`, dashed x=0 divider, dense jittered points colored by raw feature value, and Low/High colorbar as the only legend | `templateMotifCount`, `shapBeeswarmCount`, `zeroReferenceLineCount`, `colorbarSlotCount`, `sampleEncodingCount` |
| `interval_uncertainty_band` | CI/PI lower and upper columns, SEM/STD columns, survival/effect intervals | Shaded interval band or forest-style whiskers; never synthesize intervals | `referenceLineCount`, `templateMotifCount` |
| `dual_axis_hist_cumfreq_grid` | wide numeric feature table, or explicit histogram/cumulative-frequency/HPC distribution matrix cue | Case-020 HPC distribution board: 3x3 independent variable panels; each panel uses a left-axis histogram for Frequency and a right-axis blue marker line for Cumulative Frequency (%) locked to 0-105 | `templateMotifCount`, `dualAxisEncodingCount`, `multiAxisEncodingCount`, `twinAxisPanelCount`, `histogramPanelCount`, `cumulativeCurveCount`, `sampleEncodingCount` |
| `textbook_dual_axis_bar_line` | categorical x plus left/right numeric quantities, typically bar/line, porosity/strength, or dual-axis/twin-axis cues; optional left/right error columns and group/family column | Case-012 Materials Today dual-Y layer sandwich: pale blue left-axis bars with error bars, gray group dividers, warm orange spline plus raw marker error bars on the right axis, and channel-colored spines/ticks/labels | `templateMotifCount`, `dualAxisEncodingCount`, `multiAxisEncodingCount`, `groupDividerCount`, `sampleEncodingCount` |
| `nature_comms_dual_axis_bar_line` | categorical x plus neutral count/number column and focal proportion/ratio/percentage column | Case-026 Nature Comms count-vs-proportion dual axis: neutral background bars on the left axis, unsmoothed red foreground line/markers on the right axis, dashed mean reference line, and red right spine/ticks/label binding | `templateMotifCount`, `dualAxisEncodingCount`, `multiAxisEncodingCount`, `referenceLineCount`, `sampleEncodingCount` |
| `dual_axis_grouped_bar_line` | model/category x plus two or more left-axis error/performance bar metrics and one right-axis score metric such as R2 | Case-032 model-performance dual axis: Train/Test RMSE grouped bars share the left axis, Test R2 is a marker line on the right axis, the legend is merged across twin axes, and the right spine/ticks are tinted to the line color | `templateMotifCount`, `dualAxisEncodingCount`, `multiAxisEncodingCount`, `groupedBarCount`, `combinedLegend`, `sampleEncodingCount` |
| `triple_y_mechanical_grid` | five mechanical/condition panels with shared categorical x values and three same-panel metrics such as CS/FS/STS with different units | Case-054 Materials Today-style five-panel board: `GridSpec(2,6)` places three panels on the top row and two centered panels on the bottom row; each panel uses grey CS bars on the left axis, orange FS errorbar markers on the first right axis, green STS errorbar markers on a second right axis shifted outward by 45 pt, and color-linked right spines/ticks | `templateMotifCount`, `panelCount`, `multiAxisEncodingCount`, `twinAxisPanelCount`, `outwardSpineCount`, `errorbarLayerCount`, `axisColorLinked`, `sharedXAxis` |
| `dual_axis_error_sidecar` | RMSE, MAE, percent-error, new-data-point error columns | Secondary y-axis hatched error bars paired with primary predicted/experimental marks | `dualAxisEncodingCount`, `multiAxisEncodingCount` |
| `pareto_tradeoff_board` | objective columns, rank, pareto/optimal flags | Tradeoff scatter, highlighted non-dominated or optimal points only when flags or ranks exist | `inPlotExplanatoryLabelCount`, `sampleEncodingCount` |
| `association_bubble_scatter` | numeric x/y relationship plus numeric size/population column and categorical group/color column, especially AAPC/r/Spearman wording | Case-098 open-frame association bubble plot: x/y carry the focal relationship, bubble area is normalized from the size column, color encodes a categorical group with fixed palette, the group legend uses fixed-size proxy circles outside the plot, and a lower-right boxed Spearman `r` annotation summarizes association | `templateMotifCount`, `bubbleScatterCount`, `categoryCount`, `sizeEncoding`, `colorEncoding`, `proxyLegendCount`, `spearmanAnnotationCount`, `visibleSpines` |
| `nsga2_3d_pareto_front` | NSGA-II / Pareto wording plus three objective columns such as cost, carbon/GWP, and strength, with optional group/grade and selected-optimum flags | Case-064 two-panel 3D Pareto board: side-by-side `projection="3d"` axes share `view_init(elev=25, azim=-45)` and identical x/y/z limits, dense semi-transparent Pareto points are colored by strength with `viridis`, selected optima are red star markers with black edges, and a compact upper-center legend stays above the 3D data | `templateMotifCount`, `pareto3dPanelCount`, `threeDimensional`, `shared3DView`, `shared3DAxisLimits`, `paretoPointCount`, `optimalStarCount`, `externalLegendCount` |
| `pso_shap_optimization_framework` | optimization/PSO/NSGA/Pareto wording plus objective columns and explainability or model-metric columns | Framework composition: workflow context, model metric radar, Pareto tradeoff front, and SHAP/global importance explanation as linked evidence panels | `templateMotifCount`, `referenceLineCount`, `metricBoxCount`, `sampleEncodingCount` |
| `nature_radar_canonical` | physical-property metric table with two material/condition profiles, per-axis limits, and optional error columns | Case-072 Nature semiconductor-fibre radar: one square polar axis, seven normalized spokes, hidden circular polar grid replaced by dashed polygon rings/spokes, navy/crimson bipolar fills and outlines, top-layer error-bar vertices, and physical max-limit annotations outside the unit radius | `templateMotifCount`, `polarAxisCount`, `spokeCount`, `closedLoopAngles`, `polarGridReplaced`, `sandwichLayerCount`, `errorbarLayerCount`, `paletteIsBipolar`, `exportDpi` |
| `environment_radar_comparison` | environmental/model metric columns plus condition or season groups such as wet/dry, basin, scenario, or model profile | Case-066 single polar radar: six fixed spokes, closed wet/dry polygons, shared `[0, 1]` radial scale, Wet solid blue and Dry dashed orange outlines above alpha `0.25` fills, and an outside upper-right legend; scale to multi-panel only when separate basins, seasons, models, or ablation groups are present | `templateMotifCount`, `polarAxisCount`, `categoryCount`, `conditionCount`, `closedLoopAngles`, `radialScale`, `fillCount`, `lineCount`, `legendOutside` |
| `manual_cartesian_model_metric_radar` | model/algorithm rows crossed with metric columns, per-metric min/max axis scales, and exact value-label cues | Case-095 hidden-cartesian radar: equal-aspect x/y `[0, 1]` axes are turned off, dashed polygon grid levels and radial spokes are manually computed from center/radius coordinates, every metric has an explicit `axis_scales` range, each model draws a closed line+marker polygon with translucent fill, each vertex receives the original metric value, and a figure-level left legend handles long model names | `templateMotifCount`, `manualCartesianAxes`, `axisOff`, `metricCount`, `modelCount`, `axisScaleCount`, `polygonGridLevelCount`, `radialAxisCount`, `modelPolygonCount`, `vertexValueLabelCount`, `figureLegendCount` |
| `polar_comparison_signature` | cyclic angle, radar score, category-by-metric wide tables | Radar/polar comparison with hand-drawn polygon grid, muted fills, strong outline hierarchy, and layered glass markers | `templateMotifCount`, `sampleEncodingCount` |

## Source-Data Motifs

These are not visual chart motifs. They document upstream data-preparation
contracts that make later figures reproducible and auditable.

| Motif | Data cues | Source-data pattern | QA counters |
|-------|-----------|---------------------|-------------|
| `raster_zonal_statistics_export` | folder of `.tif` rasters plus Shapefile boundary, max/min/mean/std, NoData or missing-value wording, Excel/CSV export | Case-094 geospatial preprocessing table: crop each raster by vector boundary, rebuild an exact in-polygon mask with `geometry_mask`, count mask/valid/missing pixels, compute summary statistics only on valid pixels, preserve error/no-overlap rows, and export one row per raster | `exportTablePresent`, `shapefileMaskRequired`, `geometryMaskRequired`, `nodataNanChecked`, `validPixelCountColumn`, `maskPixelCountColumn`, `missingPixelCountColumn`, `summaryStatColumns`, `errorRowsPreserved` |
| `raster_temporal_nodata_fill` | GeoTIFF target raster with NoData gaps plus a complete historical same-period donor raster, usually described as complete/incomplete imagery or previous/next year repair | Case-099 geospatial source-data repair: read donor and target rasters, require aligned shape/grid metadata, convert NoData sentinels to `NaN`, fill target-missing pixels from the donor at the same row/column, preserve still-missing donor gaps as NoData, and export a metadata-preserving GeoTIFF | `rasterPreprocessingPresent`, `donorRasterRequired`, `targetRasterRequired`, `alignedShapeRequired`, `nodataToNan`, `fillMaskFormula`, `donorFillApplied`, `remainingNodataPreserved`, `geotransformPreserved`, `projectionPreserved`, `outputNodataSet` |

## Selection Rules

1. Treat motifs as evidence layers attached to implemented charts. Do not add registry keys such as `shap_beeswarm` unless a real generator exists.
2. A motif must be data-driven. SHAP, PDP, ALE, H-statistic, p-value stars, clustering trees, percent-error bars, and Pareto flags can only be rendered when columns or upstream outputs exist.
3. If the user asks for a top-journal style figure but the detected data only support a simple chart, add dense but honest overlays: direct labels, sample-size labels, summary tables, marginal distributions, reference lines, and source-data exports.
4. Legends, colorbars, metric boxes, and marginal axes are layout elements. Keep them outside plotted data regions and let render QA fail if they overlap.
5. Prefer one strong motif plus one support motif over many weak decorations. The goal is visual evidence density, not ornament.

## Phase Contracts

- Phase 1 adds semantic roles and `specialPatterns` that expose motif opportunities.
- Phase 2 writes `visualContentPlan.templateMotifs`, `layoutIntents`, `provenanceRequirements`, and minimum motif counters.
- Phase 3 uses `phases/code-gen/helpers.py` to apply motif overlays after base chart drawing and before crowding management.
- Phase 4 records motif counters in `render_qa.json` and hard-fails when a required template motif was planned but not applied.
